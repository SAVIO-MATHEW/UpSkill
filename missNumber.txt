function missingNUmber(arr) {
  let sum = 0;
  let expsum = ((arr.length + 1) * (arr.length + 2)) / 2;
  for (let num of arr) {
    sum += num;
  }
  return expsum - sum;
}

console.log("miss", missingNUmber([1, 2, 3, 4, 6])); //output 5